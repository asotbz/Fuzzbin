name: Frontend CI

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend.yml'
  pull_request:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend.yml'

defaults:
  run:
    working-directory: frontend

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run TypeScript type check
        run: npm run type-check

      - name: Run ESLint
        run: npm run lint

  test:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: frontend/coverage/
          retention-days: 7

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, test]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: frontend/dist/
          retention-days: 7

  # Playwright E2E tests - requires backend to be running
  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [build]
    # Only run E2E tests on main branch or when explicitly requested
    if: github.ref == 'refs/heads/main' || contains(github.event.pull_request.labels.*.name, 'run-e2e')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
          cache: 'pip'

      - name: Install frontend dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Install backend dependencies
        working-directory: .
        run: pip install -e ".[dev]"

      - name: Create E2E test directories
        run: |
          mkdir -p /tmp/fuzzbin-e2e/config
          mkdir -p /tmp/fuzzbin-e2e/library
          mkdir -p e2e/.auth

      - name: Start backend server
        working-directory: .
        run: |
          # Copy example config
          cp config.example.yaml /tmp/fuzzbin-e2e/config/config.yaml
          # Start server in background, redirect output to log file
          nohup fuzzbin-api > /tmp/backend.log 2>&1 &
          echo $! > /tmp/backend.pid
          # Give the server a moment to start
          sleep 3
          # Check if process is still running
          if ! kill -0 $(cat /tmp/backend.pid) 2>/dev/null; then
            echo "Backend failed to start. Logs:"
            cat /tmp/backend.log
            exit 1
          fi
        env:
          FUZZBIN_CONFIG_DIR: /tmp/fuzzbin-e2e/config
          FUZZBIN_LIBRARY_DIR: /tmp/fuzzbin-e2e/library
          FUZZBIN_API_JWT_SECRET: e2e-test-secret-key-do-not-use-in-production
          FUZZBIN_API_AUTH_ENABLED: 'true'

      - name: Start frontend dev server
        run: |
          nohup npm run dev > /tmp/frontend.log 2>&1 &
          echo $! > /tmp/frontend.pid
          sleep 3
        env:
          VITE_API_BASE_URL: http://localhost:8000

      - name: Wait for servers
        run: |
          npx wait-on http-get://localhost:8000/health http://localhost:5173 --timeout 60000 || {
            echo "=== Backend logs ==="
            cat /tmp/backend.log || true
            echo "=== Frontend logs ==="
            cat /tmp/frontend.log || true
            exit 1
          }

      - name: Run Playwright tests
        run: npm run test:e2e
        env:
          VITE_API_BASE_URL: http://localhost:8000
          E2E_TEST_USERNAME: admin
          E2E_TEST_PASSWORD: changeme

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 7

      - name: Upload test screenshots
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-screenshots
          path: frontend/test-results/
          retention-days: 7
